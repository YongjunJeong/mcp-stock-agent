# ── 데이터 수집 ───────────────────────────────────────
pykrx>=1.2.4
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
yfinance>=0.2.40

# ── 기술적 지표 ───────────────────────────────────────
# RSI/MACD/볼린저는 mcp_server/indicators.py에서 pandas로 직접 계산합니다.
# (pandas-ta는 0.4 계열부터 numba를 필수로 요구해 --no-deps 설치와 충돌)
pandas>=2.2,<3.0
numpy>=1.26,<3.0

# ── AI / Gemini ───────────────────────────────────────
google-genai>=1.0.0

# ── MCP ──────────────────────────────────────────────
# 2.x에서 FastMCP -> MCPServer로 바뀌고 저수준 데코레이터 API가 제거됐습니다.
# 메이저 업그레이드가 조용히 서버를 깨뜨리지 않도록 상한을 둡니다.
mcp>=2.2,<3.0

# ── Slack ─────────────────────────────────────────────
slack-bolt>=1.18.0
slack-sdk>=3.27.0

# ── 스케줄러 ──────────────────────────────────────────
apscheduler>=3.10.0

# ── 유틸리티 ──────────────────────────────────────────
# ── Database ───────────────────────────────────────
aiosqlite>=0.20.0

python-dotenv>=1.0.0
